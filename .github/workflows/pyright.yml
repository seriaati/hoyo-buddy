name: Type check
on:
  workflow_dispatch:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  pyright:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Install uv
        uses: astral-sh/setup-uv@v8.0.0

      - name: Install Project
        run: |
          uv sync --frozen --no-dev --group type-check
          uv pip uninstall uvloop
          echo "$PWD/.venv/bin" >> $GITHUB_PATH

      - name: Run Pyright
        run: |
          uv run pyright
